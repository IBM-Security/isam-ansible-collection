---
# configure
#   configure reverse proxy aac
- hosts: "{{ hosts | default('all')}}"
  gather_facts: no
  roles:
    - role: ibm.isam.web.config_reverseproxy_aac
      tags: config_reverseproxy_aac
