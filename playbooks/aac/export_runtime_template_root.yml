---
# export
#   export runtime template root
#   Example:
- hosts: "{{ hosts | default('all')}}"
  gather_facts: no
  roles:
    - role: ibm.isam.aac.export_runtime_template_root
      tags: export_runtime_template_root
