---
# Task
#   description
#   Example:
#      iterator:
- name: Task name
  hosts: "{{ hosts | default('all') }}"
  gather_facts: False
  roles:
    - role: ibm.isam.base.playbook
      tags: playbook
