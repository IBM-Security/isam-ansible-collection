---
# Configure
#   configure signer certificates
- hosts: "{{ hosts | default('all')}}"
  gather_facts: no
  roles:
    - role: ibm.isam.base.configure_signer_certificates
      tags: configure_signer_certificates
