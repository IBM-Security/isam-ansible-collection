---
- name: Export application logs
  hosts: all
  gather_facts: False
  collections:
    - ibm.isam
  tasks:
    - name: "Export application logs"
      include_role:
        name: export_application_logs
