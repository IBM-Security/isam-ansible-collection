---
- name: Export application logs
  hosts: all
  gather_facts: false
  collections:
    - ibm.isam
  tasks:
    - name: Export application logs
      ansible.builtin.include_role:
        name: export_application_logs
