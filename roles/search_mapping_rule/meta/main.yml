---
galaxy_info:
  author: IBM
  description: Role that searches for a Mapping Rule (for ID lookup)
  company: IBM

  license: Apache

  min_ansible_version: "2.2"
  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - mappingrule
    - search

dependencies:
  - ibm.isam.common_handlers
