---
galaxy_info:
  author: IBM
  description: Role to import HTTP Transformation Rule
  company: IBM

  license: Apache

  min_ansible_version: "2.2"

  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - httptransformation
    - import

dependencies:
  - ibm.isam.common_handlers
