---
galaxy_info:
  author: IBM
  description: Role to set a junction to a reverse proxy
  company: IBM

  license: Apache

  min_ansible_version: "2.2"
  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - reverseproxy
    - junction
    - set

dependencies:
  - ibm.isam.common_handlers
