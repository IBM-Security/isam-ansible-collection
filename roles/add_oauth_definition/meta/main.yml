---

galaxy_info:
  author: IBM
  description: Role to add API Protection Definition (for OAuth 2.0)
  company: IBM

  license: Apache

  min_ansible_version: "2.2"

  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - oauth
    - add

dependencies:
  - ibm.isam.common_handlers
