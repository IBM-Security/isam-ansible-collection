---
galaxy_info:
  # role_name: isam.aac.configure_mapping_rules
  namespace: ibm # if absent, author is used instead
  author: IBM
  description: Role to configure runtime template root content
  company: IBM

  license: Apache

  min_ansible_version: "2.2"

  galaxy_tags:
    - isam
    - ibm
    - configure
    - runtimetemplateroot

dependencies:
  - ibm.isam.common_handlers
