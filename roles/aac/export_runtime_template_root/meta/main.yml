---
galaxy_info:
  #role_name: isam.aac.export_runtime_template_root
  namespace: ibm
  author: IBM
  description: Role to export runtime template root content
  company: IBM

  license: Apache

  min_ansible_version: '2.9'

  galaxy_tags:
    - isam
    - ibm
    - export
    - runtime_template_root

dependencies:
  - ibm.isam.common_handlers
