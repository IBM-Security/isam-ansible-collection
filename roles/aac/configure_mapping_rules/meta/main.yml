---
galaxy_info:
  namespace: ibm # if absent, author is used instead
  author: IBM
  description: Role to configure mapping rules
  company: IBM

  license: Apache

  min_ansible_version: "2.9"

  galaxy_tags:
    - isam
    - ibm
    - configure
    - mapping_rules

dependencies:
  - ibm.isam.common_handlers
