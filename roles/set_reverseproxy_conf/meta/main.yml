---
galaxy_info:
  author: IBM
  description: Role to set values in reverse proxy configuration file with given stanza/entries
  company: IBM

  license: Apache

  min_ansible_version: "2.2"
  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - reverseproxy
    - web
    - configuration
    - set

dependencies:
  - ibm.isam.common_handlers
