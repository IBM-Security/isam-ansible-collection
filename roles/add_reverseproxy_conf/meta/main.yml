---

galaxy_info:
  author: IBM
  description: Role to add to reverse proxy configuration file with given stanza/entries
  company: IBM

  license: Apache

  min_ansible_version: "2.2"

  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - reverseproxy
    - web
    - configuration
    - add

dependencies:
  - ibm.isam.common_handlers
