---
galaxy_info:
  author: IBM
  description: Role Configures federated directory
  company: IBM

  license: Apache

  min_ansible_version: "2.2"

  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - policyserver
    - federation
    - directory
    - config

dependencies:
  - ibm.isam.common_handlers
