---

# Default variables for create ssl certificate database
create_certificate_databases: [] # noqa: var-naming[no-role-prefix]
# limit execution at runtime to filter for kdb_name's
kdb_name: "{{ item.kdb_name }}" # noqa: var-naming[no-role-prefix]
