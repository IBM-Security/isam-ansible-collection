---

# Default variables for configuration of runtime tuning parameters
runtime_tunings: [] # noqa: var-naming[no-role-prefix]
# Default variables to filter for configuration of specific runtime tuning parameters at runtime
option: "{{ item.option }}" # noqa: var-naming[no-role-prefix]
runtime_action: "{{ item.runtime_action }}" # noqa: var-naming[no-role-prefix]
