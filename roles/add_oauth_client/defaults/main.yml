---

# Provide the name of the API Definition Name (not the id, name will be used for lookup)
# add_oauth_client_definitionName

# Provide the following for this role to work
# add_oauth_client_name
# add_oauth_client_companyName

# The following are valid defaults and can be overriden as needed
add_oauth_client_redirectUri:
add_oauth_client_companyUrl:
add_oauth_client_contactPerson:
add_oauth_client_contactType:
add_oauth_client_email:
add_oauth_client_phone:
add_oauth_client_otherInfo:
add_oauth_client_extProperties:
# Might be best to override these - rather than allow them to randomly generate
add_oauth_client_clientId:
add_oauth_client_clientSecret:
add_oauth_client_requirePkce:
add_oauth_client_encryptionDb:
add_oauth_client_encryptionCert:
add_oauth_client_jwksUri:
