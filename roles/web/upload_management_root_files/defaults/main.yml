---
# Default variables for uploading management root files
management_root_files: [] # noqa: var-naming[no-role-prefix]
web_management_root_name: "{{ item.file | basename }}"
inst_name: "{{ item.instance_id }}" # noqa: var-naming[no-role-prefix]
