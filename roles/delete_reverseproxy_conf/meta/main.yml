---
galaxy_info:
  author: IBM
  description: Role to delete entry from reverse proxy configuration file
  company: IBM

  license: Apache

  min_ansible_version: "2.2"
  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - reverseproxy
    - web
    - configuration
    - delete

dependencies:
  - ibm.isam.common_handlers
