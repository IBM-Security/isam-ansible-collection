---
galaxy_info:
  author: IBM
  description: Role that will install a firmware into and appliance
  company: IBM

  license: Apache

  min_ansible_version: "2.2"

  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - firmware
    - upgrade
    - install

dependencies:
  - ibm.isam.common_handlers
