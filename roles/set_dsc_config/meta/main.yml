---
galaxy_info:
  author: IBM
  description: Role to Configure DSC
  company: IBM

  license: Apache

  min_ansible_version: "2.2"
  platforms:
    - name: GenericLinux
      versions:
        - all

  galaxy_tags:
    - isam
    - ibm
    - dsc
    - configuration

dependencies:
  - ibm.isam.common_handlers
